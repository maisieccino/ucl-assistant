const randomstring = require("randomstring");
const moment = require("moment");
const passport = require("koa-passport");

module.exports = async ctx => {
  passport.authenticate("oauth2", { failure });
};
